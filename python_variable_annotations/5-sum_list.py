#!/usr/bin/env python3
"Takes a list of floats as argument and returns the sum as a float."
from typing import List


def sum_list(input_list: List[float]) -> float:
    "returns sum of list as a float"
    return sum(input_list)
